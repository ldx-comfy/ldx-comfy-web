# Frontend environment variables for Astro app
# Base URL of the backend API (without trailing slash)
# Example for local backend:
VITE_API_BASE_URL=http://127.0.0.1:1145

# Notes:
# - Do NOT add a trailing slash.
# - Client will auto-append /api/v1 when calling API.
# - In production, set to your backend origin, e.g.:
# VITE_API_BASE_URL=https://api.example.com